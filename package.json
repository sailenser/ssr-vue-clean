{
  "name": "vite-ssr-balovstvo",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "node server",
    "build": "npm run build:client && npm run build:server",
    "build:client": "vite build --outDir dist-ssr/client",
    "build:server": "vite build --outDir dist-ssr/server --ssr src/entry-server.js"
  },
  "dependencies": {
    "@vue/server-renderer": "^3.5.21",
    "dotenv": "^17.2.2",
    "express": "^5.1.0",
    "vue": "^3.5.18"
  },
  "devDependencies": {
    "@types/node": "^24.4.0",
    "@vitejs/plugin-vue": "^6.0.1",
    "@vue/tsconfig": "^0.7.0",
    "typescript": "~5.8.3",
    "vite": "^7.1.2",
    "vue-tsc": "^3.0.5"
  }
}
